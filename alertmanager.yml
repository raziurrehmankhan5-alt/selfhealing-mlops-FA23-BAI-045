global:
  resolve_timeout: 1m

route:
  receiver: 'jenkins-rollback-webhook'
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: 'jenkins-rollback-webhook'
    webhook_configs:
      - url: 'http://13.63.198.254:8080/generic-webhook-trigger/invoke?token=ROLLBACK_773134_TOKEN'
        send_resolved: true
        http_config:
          basic_auth:
            username: razi
            password: 110f3902b08db05dee632e49a9b4ea3dc8

      
